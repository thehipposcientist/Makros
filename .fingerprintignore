# Read by @expo/fingerprint (EAS "Computing project fingerprint" step).
# Excludes large STATIC bundled media from the hash so the fingerprint stops
# spending minutes reading ~600 MB of .mp4/.jpg on every build.
#
# These still ship in the binary — this only affects the fingerprint/runtime
# version EAS auto-derives. A manual store build (make ios / eas build) does
# not depend on that, so excluding them is safe. If you move to fingerprint-
# gated OTA updates and need asset changes to bump the runtime version, drop
# the relevant lines.
assets/exercise-videos/
assets/images/
assets/exercise-demos/
assets/test/

# Regenerated by `pod install` during the build — not a meaningful fingerprint input.
ios/Pods/
